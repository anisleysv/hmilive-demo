const APP_NAME = "ServerConnect_API"; // Name to list the app inside the monitor display, it must match the project's folder name inside htdocs; to be set from one app to another.
const APP_PATH = "app.js"; //The name could remain fixed if the file name of the main app's script was updated to match.
const AUTORUN = true; //true only if you would like the monitor to run the app automatically when the monitor is started, else it will wait to be started from the monitor display.
const SHOW_QR_IN_MONITOR = true;